export interface PlaylistInfo {
  name: string,
  id: string,
  author: string
}

export interface PlaylistVideo {
  name: string,
  id: string,
  author: string,
  length: number,
  thumbnail: string
}
